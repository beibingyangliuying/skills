---
name: repo-workflow
description: 当任务涉及仓库环境确认、Python 命令执行、脚本运行或 `pytest` 验证时使用，用于保证命令在正确环境中执行，不用于一般性的代码风格或接口设计约束。
---

# Repo Workflow

## 何时使用

当任务包含以下内容时使用本 skill：

- 运行 Python 命令
- 执行测试
- 运行脚本
- 确认虚拟环境或依赖环境

## 核心目标

在正确的仓库环境中执行命令，并以最小代价验证修改结果。

## 边界说明

- MUST: 本 skill 只约束环境确认、命令执行与验证流程，不替代代码风格、数据处理或发布兼容性规则。
- SHOULD: 若任务只是静态改写代码而不需要运行命令，本 skill 可以只提供最小化的验证判断，不必强行扩展工作流步骤。

## 执行规则

### 1. 环境确认

- MUST: 在运行本仓库相关 Python 命令前，先查看仓库已有的环境约定，例如 `README.md`、现有脚本、CI 配置、任务脚本或命令示例。
- SHOULD: 若仓库未给出更明确约定，再尝试常见的本地虚拟环境入口，例如 `.venv/Scripts/Activate.ps1` 或等效方式。
- MUST: 若虚拟环境已成功激活，后续所有以 `uv` 开头的命令都必须显式追加 `--active`，包括 `uv run`、`uv pip`、`uv sync` 等常见形式。
- SHOULD: 将 `--active` 视为已激活环境下 `uv` 命令的默认写法，以明确使用当前激活环境，避免落到错误解释器或环境解析逻辑。
- MUST: 若无法确认可用环境，必须明确告知用户。

### 2. 测试执行

- MUST: 默认使用 `pytest`。
- SHOULD: 修改公开函数、核心逻辑、数据处理流程或 bug 修复后，优先运行最相关的最小测试集。
- SHOULD: 能跑单测时优先跑单测，避免无必要的全量测试。
- MUST: 若未运行测试，必须明确说明原因。
- MAY: 为保持测试简洁，在非常小的纯函数中补充 doctest，但不替代主要测试策略。

### 3. 脚本与命令验证

- SHOULD: 先做最小可验证修改，再运行最小必要命令验证。
- MUST: 若已成功激活虚拟环境，则后续实际执行的所有 `uv` 命令都必须包含 `--active`。
- SHOULD: 若任务仅涉及静态改写且无法安全运行命令，应明确说明未执行命令验证。
- MUST: 不要假装命令已经成功运行；只报告实际执行和实际结果。
- MUST: 汇报 `uv` 命令执行结果时，必须基于实际执行过且包含 `--active` 的命令；若环境未成功激活，不得假设 `uv` 已使用正确环境。

## 交付检查清单

在完成任务前，检查：

- 是否已先查看仓库现有环境约定
- 若已激活环境，是否为所有实际执行的 `uv` 命令附加了 `--active`
- 是否运行了相关 `pytest`
- 是否如实汇报了未执行测试的原因
- 是否避免把一般代码改写错误地扩展成工作流任务
